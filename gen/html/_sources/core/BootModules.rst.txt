
##########################
  Boot Modules  
##########################
 
.. toctree::
   :titlesonly:
   :glob:

   Boot/*

 
.. list-table::  
   :widths: 20 80 
   :header-rows: 1 

   * - Module 
     - Description 

   * - BootLoad 
     - loads the Inner Core from disk or serial. 
   * - HAL.risc5.qemu 
     - provides a Hardware Abstraction Layer for RISC5 on QEMU. 
   * - HAL.arm32.qemu 
     - provides a Hardware Abstraction Layer for 32-bit arm. 
   * - HAL.arm64.qemu 
     - provides a Hardware Abstraction Layer for aarch64. 
   * - HAL.arm64.pbpro 
     - provides a Hardware Abstraction Layer for aarch64 on the Pinebook Pro. 
   * - HAL.arm64.pphone 
     - provides a Hardware Abstraction Layer for aarch64 on the Pinephone. 
   * - HAL.cortex4.qemu 
     - provides a Hardware Abstraction Layer for cortex-m4. 
   * - HAL.cortex0.qemu 
     - provides a Hardware Abstraction Layer for cortex-m0. 
   * - HAL.cortex4.ptime 
     - provides a Hardware Abstraction Layer for cortex-m4 on the PineTime. 
   * - HAL.cortex0.pico 
     - provides a Hardware Abstraction Layer for cortex-m0 on the Raspberry Pi Pico. 
   * - HAL.x8664.qemu 
     - provides a Hardware Abstraction Layer for x86_64. 
   * - HAL.riscv32.qemu 
     - provides a Hardware Abstraction Layer for 32-bit riscv. 
   * - HAL.riscv64.qemu 
     - provides a Hardware Abstraction Layer for 64-bit riscv. 
   * - HAL.x8664.lin 
     - provides a Hardware Abstraction Layer for x86_64 in Linux userspace. 
   * - HAL.x8664.win 
     - provides a Hardware Abstraction Layer for x86_64 on Windows. 
   * - HAL.x8664.osx 
     - provides a Hardware Abstraction Layer for x86_64 in OSX userspace. 

